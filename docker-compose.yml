services:
  aura-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      AURA_DATABASE_PATH: /app/data/aura.db
      AURA_JWT_SECRET: dev-jwt-secret-change-me
      AURA_JWT_ALGORITHM: HS256
      AURA_JWT_EXP_MINUTES: "60"
    volumes:
      - ./data:/app/data
